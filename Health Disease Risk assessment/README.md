install the requirements.txt file with python work space created.

pip install -r requirements.txt

go to the doctor-Dashboard(FrontEnd)/templates folder and run the app.py

it'll take us to the actual front end.

FOR PYTHON SCRIPTS, open the appropriate disease folders and visit src AND Notebooks folders